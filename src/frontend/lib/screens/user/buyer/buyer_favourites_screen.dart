import 'package:flutter/material.dart';

class BuyerFavouritesScreen extends StatelessWidget {
  const BuyerFavouritesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Text("Favourites")));
  }
}
